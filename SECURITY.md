# Security policy

Do not report vulnerabilities in public issues. Send a private report through GitHub Security Advisories for this repository with impact, affected version and reproduction steps.

Never expose JWT secrets, MinIO credentials or application delivery tokens to browser code. Rotate any credential that may have been disclosed. Security fixes are prioritized for the latest `main` revision.
